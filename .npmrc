# Nest and Prisma rely on peer resolution that strict-peer-deps rejects.
strict-peer-dependencies=false
auto-install-peers=true

# Keep the payment app's dependency tree auditable: no phantom deps.
shamefully-hoist=false

# Native build scripts must be opted into explicitly (pnpm >= 10).
enable-pre-post-scripts=true
